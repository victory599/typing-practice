# 测试报告

- **执行日期：** 2026-07-22（首轮）；体验迭代后文档同步同日
- **版本：** 含 `fa6f02a` 体验优化
- **执行人：** Cursor

## 1. 摘要

| 结果 | 数量 |
|------|------|
| 首轮 API/构建通过 | 10（T01–T10） |
| 首轮未完整点验 | 2（T11、T12） |
| 体验迭代新增用例 | T13–T20（以代码落地为准；建议本地再点验） |

`npm run build` 曾通过；数据迁移语义验证通过。体验迭代后请重启 `npm run dev` 再验批量删除等服务端改动。

## 2. 用例结果（首轮）

| 编号 | 结果 | 说明 |
|------|------|------|
| T01 | 通过 | `GET /api/health` → `{"ok":true}` |
| T02 | 通过 | 返回固定 config 路径与默认 data 路径 |
| T03 | 通过（语义已更新） | 现为「文件不存在才灌范文」；空数组保持为空 |
| T04–T10 | 通过 | 见首轮记录 |
| T11–T12 | 建议复测 | 练习页与错误着色 |

## 3. 体验迭代新增（建议复测）

| 编号 | 说明 |
|------|------|
| T13 | 随机选题 |
| T14 | 结算「换一篇」随机换文 |
| T15 | 「去词库」跳转 |
| T16 | 默认路径下按钮置灰 |
| T17 | 批量删除（`POST /api/texts/bulk-delete`） |
| T18 | 全部删除且不自动灌回范文 |
| T19 | 空格不为 `·` |
| T20 | 超长标题下拉不撑破布局 |

## 4. 缺陷

- 曾出现：前端已热更新、文件服务未重启导致批量删除 404 → 已改为 `POST /api/texts/bulk-delete`，需重启服务验证。

## 5. 结论

功能已按体验需求落地并写入文档；请本地对 T13–T20 快速点验后可视为本轮关闭。
