# 测试报告

- **执行日期：** 2026-07-22
- **版本：** 开发首版提交 `72ff525` 及后续文档提交
- **执行人：** 开发代理

## 1. 摘要

| 结果 | 数量 |
|------|------|
| 通过 | 10 |
| 失败 | 0 |
| 阻塞/未测（需完整浏览器手工） | 2（T11、T12 建议用户本地再确认） |

`npm run build` 通过；本机文件 API 用例全部通过；数据目录迁移验证为移动语义。

## 2. 用例结果

| 编号 | 结果 | 说明 |
|------|------|------|
| T01 | 通过 | `GET /api/health` → `{"ok":true}` |
| T02 | 通过 | 返回固定 config 路径与默认 data 路径 |
| T03 | 通过 | 首次写入 3 篇范文 |
| T04 | 通过 | 成功创建「测试」文本 |
| T05 | 通过 | 成绩写入并可列出 |
| T06 | 通过 | 清空后列表为空 |
| T07 | 通过 | 迁到 `tmp-data-migrate` 后旧 `data/` 无业务文件；迁回默认成功 |
| T08 | 通过 | `PUT /api/settings` 代码路径固定保留 `dataPath`（设计如此） |
| T09 | 通过 | `vue-tsc -b && vite build` 成功 |
| T10 | 通过 | WPM/准确率实现与 PRD 注释一致 |
| T11 | 未在本报告中做完整浏览器点击验收 | 构建与路由已就绪；请本地 `npm run dev` 打开确认 |
| T12 | 未在本报告中做完整浏览器点击验收 | 引擎逻辑已实现；请本地确认观感 |

## 3. 缺陷

无阻断缺陷。建议用户本地对 T11/T12 做一次界面确认。

## 4. 结论

服务与数据层达到可交付标准；界面核心路径建议用户启动后快速点验。修复项：无。
